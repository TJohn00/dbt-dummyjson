version: 2

sources:
  - name: raw
    database: "{{ env_var('DBT_DATABASE', '') }}"
    schema: "{{ env_var('DBT_RAW_SCHEMA', '') }}"
    tables:
      - name: products
      - name: product_reviews
      - name: categories
      - name: users
      - name: carts
      - name: cart_items